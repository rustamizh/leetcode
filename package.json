{
  "name": "leetcode",
  "version": "1.0.0",
  "description": "package for solving leetcode problems",
  "main": "index.js",
  "scripts": {
    "start": "concurrently --kill-others \"npm run tcs\" \"npm run nodemon\"",
    "tcs": "tsc -w",
    "nodemon": "nodemon -q -w dist dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.2",
    "nodemon": "^3.0.3",
    "typescript": "^5.3.3"
  }
}
